from setuptools import setup, find_packages

setup(
    name="calculator-service",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[],
    author="Nikhil",
    description="Industry-level calculator service",
)
